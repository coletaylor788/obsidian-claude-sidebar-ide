{
  "name": "obsidian-claude-sidebar-ide",
  "version": "1.8.2",
  "description": "Run Claude Code in your sidebar.",
  "main": "main.js",
  "scripts": {
    "dev": "bun run esbuild.config.ts --watch",
    "build": "bun run esbuild.config.ts --prod",
    "deploy": "bun run build && bun run deploy.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "tsc --noEmit && bun test && bun run build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "ws": "^8.19.0"
  }
}
